export type IFirstCardData = {
  description: string;
};

export type ISecondCardData = {
  name: string;
  description: string;
};

export type IPricingCardPhoto = {
  photo: 'wheel' | 'engine' | 'brakes' | 'transmisie' | 'oil';
};
